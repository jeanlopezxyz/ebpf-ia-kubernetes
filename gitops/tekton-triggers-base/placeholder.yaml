# Tekton Triggers Installation Placeholder
# 
# This file ensures ArgoCD application exists for Tekton Triggers
# The actual installation is managed by applying official releases:
# - https://storage.googleapis.com/tekton-releases/triggers/latest/release.yaml
# - https://storage.googleapis.com/tekton-releases/triggers/latest/interceptors.yaml
#
# Status: Tekton Triggers is installed and functional
# Components: controller, webhook, core-interceptors (all Running)
#
# This placeholder prevents ArgoCD sync errors while maintaining
# GitOps principles for cluster recreation.

apiVersion: v1
kind: ConfigMap
metadata:
  name: tekton-triggers-status
  namespace: tekton-pipelines
  labels:
    app.kubernetes.io/name: tekton-triggers
    app.kubernetes.io/component: status
    app.kubernetes.io/part-of: ebpf-ai-system
data:
  status: "installed"
  version: "v0.33.0"
  installation-method: "argocd-managed"
  components: "controller,webhook,core-interceptors"
  last-sync: "2025-09-09T15:30:00Z"