# pipelineRuns enabled by default
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: helm-charts-dependency-update
  namespace: ebpf-security
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/name: tekton-ci
    app.kubernetes.io/instance: tekton-ci
    app.kubernetes.io/component: pipelinerun-helm-charts
    app.kubernetes.io/part-of: ebpf-ai-system
spec:
  pipelineRef:
    name: helm-charts-ci
  workspaces:
    - name: shared-data
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
  params:
    - name: git-url
      value: https://github.com/jeanlopezxyz/ebpf-ia-gitops.git
    - name: git-revision
      value: main
    - name: charts-path
      value: helm/charts
    - name: commit-message
      value: "Auto-update Helm chart dependencies and .tgz files"
