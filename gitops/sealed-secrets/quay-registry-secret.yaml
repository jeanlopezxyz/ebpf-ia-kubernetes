apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: quay-registry-secret
  namespace: ebpf-security
  labels:
    app.kubernetes.io/name: quay-registry
    app.kubernetes.io/part-of: ebpf-ai-platform
    app.kubernetes.io/component: secret
spec:
  encryptedData:
    # Docker registry credentials for Quay.io
    .dockerconfigjson: AgBwK7...   # Encrypted docker config here
  template:
    metadata:
      name: quay-registry-secret
      namespace: ebpf-security
      labels:
        app.kubernetes.io/name: quay-registry
        app.kubernetes.io/part-of: ebpf-ai-platform
    type: kubernetes.io/dockerconfigjson